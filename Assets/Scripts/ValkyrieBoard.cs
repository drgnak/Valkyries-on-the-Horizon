using UnityEngine;
using System.Collections;

public class ValkyrieBoard : MonoBehaviour {
	
	public Texture2D background;

	// Use this for initialization
	void Start () {
		
	}

	void OnGUI () {
		//GUI.Label(new Rect(0, 0, 1280, 720), new GUIContent(background));
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
